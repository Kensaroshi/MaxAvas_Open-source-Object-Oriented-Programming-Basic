using System;
using using System.Collections.Generic; // **หมายเหตุ:** ในโค้ดจริงบรรทัดนี้พิมพ์เกินมา ("using" ซ้ำ)
using System.Text;

namespace ConsoleApplication1
{
    class Program
    {
        static void Main(string[] args)
        {
            Console.Read(); // อ่านอักขระตัวแรก แต่ไม่เก็บค่าไว้ในตัวแปร
        }
    }
}